# File Integrity Checker

This project is a Python-based tool that checks whether a file has been modified.

It generates a hash value of a file and stores it. Later the program generates the hash again and compares both values to detect any changes.

Technologies Used:

* Python
* hashlib library

Files in this project:

* file\_integrity\_checker.py
* hash\_store.txt
* sample.txt

